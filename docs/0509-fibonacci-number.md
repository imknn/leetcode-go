# 509. 斐波那契数

## 题目信息

| 属性 | 值 |
|------|-----|
| 题号 | [509. 斐波那契数](https://leetcode.cn/problems/fibonacci-number/) |
| 难度 | 简单 |
| 分类 | dynamic_programming |
| 标签 | 递归、记忆化搜索、数学、动态规划 |
| 文件 | `dynamic_programming/code_509.go` |

## 题目描述

**斐波那契数** （通常用 `F(n)` 表示）形成的序列称为 **斐波那契数列**。该数列由 `0` 和 `1` 开始，后面的每一项数字都是前面两项数字的和。也就是：
```
F(0) = 0，F(1) = 1
F(n) = F(n - 1) + F(n - 2)，其中 n > 1
```

给定 `n`，请计算 `F(n)`。

**示例 1：**
```
输入：n = 2
输出：1
解释：F(2) = F(1) + F(0) = 1 + 0 = 1
```

**示例 2：**
```
输入：n = 3
输出：2
解释：F(3) = F(2) + F(1) = 1 + 1 = 2
```

**示例 3：**
```
输入：n = 4
输出：3
解释：F(4) = F(3) + F(2) = 2 + 1 = 3
```

**提示：**
- `0 <= n <= 30`

## 解题算法

### 方法：动态规划（滚动变量）

**核心思路：**
斐波那契数的定义即为递推关系。使用两个变量 `m`、`n` 滚动记录前两个状态，从第 2 项开始迭代计算。

**边界条件：**
- `F(0) = 0`
- `F(1) = F(2) = 1`

**代码实现：**
```go
func fib(N int) int {
	m, n := 1, 1
	if N == 0 {
		return 0
	}
	if N == 1 || N == 2 {
		return 1
	}
	for i := 2; i < N; i++ {
		m, n = n, m+n
	}
	return n
}
```

**复杂度分析：**
- 时间复杂度：O(n)，循环 n-2 次
- 空间复杂度：O(1)，只使用两个变量
