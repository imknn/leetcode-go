# 268. 丢失的数字

## 题目信息

| 属性 | 值 |
|------|-----|
| 题号 | [268. 丢失的数字](https://leetcode.cn/problems/missing-number/) |
| 难度 | Easy |
| 分类 | math |
| 标签 | 数学、数组 |
| 文件 | `math/code_268.go` |

## 题目描述

给定一个包含 [0, n] 中 n 个数的数组 nums，找出 [0, n] 这个范围内没有出现在数组中的那个数。

**示例 1：**
```
输入：nums = [3,0,1]
输出：2
解释：n = 3，因为有 3 个数字，所以所有的数字都在范围 [0,3] 内。2 是丢失的数字，因为它没有出现在 nums 中。
```

**示例 2：**
```
输入：nums = [0,1]
输出：2
解释：n = 2，因为有 2 个数字，所以所有的数字都在范围 [0,2] 内。2 是丢失的数字，因为它没有出现在 nums 中。
```

**示例 3：**
```
输入：nums = [9,6,4,2,3,5,7,0,1]
输出：8
解释：n = 9，因为有 9 个数字，所以所有的数字都在范围 [0,9] 内。8 是丢失的数字。
```

## 解题算法

### 方法：数学求和差值法

**核心思路：**
利用等差数列求和公式计算 0 到 n 的理论总和，再减去数组中所有实际元素的和，差值即为缺失的数字。初始化 sum 为 n（即数组长度），然后在遍历数组时累加索引值并减去元素值，避免整数溢出。

**代码实现：**
```go
func missingNumber(nums []int) int {
	sum := len(nums)
	for i, v := range nums {
		sum += i - v
	}
	return sum
}
```

**复杂度分析：**
- 时间复杂度：O(n)，只需遍历一次数组
- 空间复杂度：O(1)，只使用了常数个额外变量
