# 10. 正则表达式匹配

## 题目信息

| 属性 | 值 |
|------|-----|
| 题号 | [10. 正则表达式匹配](https://leetcode.cn/problems/regular-expression-matching/) |
| 难度 | 困难 |
| 分类 | dynamic_programming |
| 标签 | 递归、字符串、动态规划 |
| 文件 | `dynamic_programming/code_10.go` |

## 题目描述

给你一个字符串 `s` 和一个字符规律 `p`，请你来实现一个支持 `'.'` 和 `'*'` 的正则表达式匹配。

- `'.'` 匹配任意单个字符
- `'*'` 匹配零个或多个前面的那一个元素

所谓匹配，是要涵盖 **整个** 字符串 `s` 的，而不是部分字符串。

**示例 1：**
```
输入：s = "aa", p = "a"
输出：false
解释："a" 无法匹配 "aa" 整个字符串。
```

**示例 2：**
```
输入：s = "aa", p = "a*"
输出：true
解释：因为 '*' 代表可以匹配零个或多个前面的那一个元素，在这里前面的元素就是 'a'。因此，字符串 "aa" 可被视为 'a' 重复了一次。
```

**示例 3：**
```
输入：s = "ab", p = ".*"
输出：true
解释：".*" 表示零个或多个任意字符。
```

**提示：**
- `1 <= s.length <= 20`
- `1 <= p.length <= 30`
- `s` 只包含从 `a-z` 的小写字母
- `p` 只包含从 `a-z` 的小写字母，以及字符 `.`
- 保证每次出现字符 `*` 时，前面都匹配到有效的字符

## 解题算法

### 方法：动态规划

**核心思路：**
定义 `dp[i][j]` 表示字符串 `s` 的前 `i` 个字符与模式 `p` 的前 `j` 个字符是否匹配。

**状态转移方程：**
1. 当 `p[j-1] == '*'` 时：
   - `*` 匹配零次：`dp[i][j] = dp[i][j-2]`
   - `*` 匹配一次或多次：若 `p[j-2]` 与 `s[i-1]` 匹配，则 `dp[i][j] = dp[i][j] || dp[i-1][j]`
2. 当 `p[j-1]` 是普通字符或 `.` 时：
   - `dp[i][j] = dp[i-1][j-1]`

**初始条件：**
- `dp[0][0] = true`（空串匹配空模式）
- 对于空串 `s`，若 `p[j-1] == '*'`，则 `dp[0][j] = dp[0][j-2]`

**代码实现：**
```go
func isMatch(s string, p string) bool {
	m, n := len(s), len(p)
	// dp[i][j]: s 前 i 个字符是否匹配 p 前 j 个字符
	dp := make([][]bool, m+1)
	for i := range dp {
		dp[i] = make([]bool, n+1)
	}
	dp[0][0] = true

	// 空 s 匹配 p 中 '*' 使前面对应字符被消掉的情况
	for j := 2; j <= n; j++ {
		if p[j-1] == '*' {
			dp[0][j] = dp[0][j-2]
		}
	}

	for i := 1; i <= m; i++ {
		for j := 1; j <= n; j++ {
			if p[j-1] == '*' {
				// '*' 匹配零次：dp[i][j-2]
				dp[i][j] = dp[i][j-2]
				// '*' 匹配一次或多次：s[i-1] 必须匹配 p[j-2]
				if p[j-2] == '.' || p[j-2] == s[i-1] {
					dp[i][j] = dp[i][j] || dp[i-1][j]
				}
			} else if p[j-1] == '.' || p[j-1] == s[i-1] {
				dp[i][j] = dp[i-1][j-1]
			}
		}
	}
	return dp[m][n]
}
```

**复杂度分析：**
- 时间复杂度：O(m * n)，其中 m 为字符串 s 的长度，n 为模式 p 的长度
- 空间复杂度：O(m * n)，需要 m * n 的二维数组存储状态
