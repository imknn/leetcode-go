# 283. 移动零

## 题目信息

| 属性 | 值 |
|------|-----|
| 题号 | [283. 移动零](https://leetcode.cn/problems/move-zeroes/) |
| 难度 | Easy |
| 分类 | array |
| 标签 | two pointers |
| 文件 | `array/code_283.go` |

## 题目描述

给定一个数组 `nums`，编写一个函数将所有 `0` 移动到数组的末尾，同时保持非零元素的相对顺序。

请注意，必须在不复制数组的情况下原地对数组进行操作。

**示例 1：**

```
输入：nums = [0,1,0,3,12]
输出：[1,3,12,0,0]
```

**示例 2：**

```
输入：nums = [0]
输出：[0]
```

## 解题算法

### 方法：找到零就与右侧非零交换

**核心思路：**
遍历数组，当遇到 0 时，向右查找最近的非零元素，将其交换到当前位置。如果右侧已无非零元素，说明剩余元素全为零，减小有效长度即可。这种方式保持了非零元素的相对顺序。

**代码实现：**
```go
func moveZeroes(nums []int) {
	length := len(nums)
	for x := 0; x < length; x++ {
		if nums[x] == 0 {
			y := x + 1
			for y < length && nums[y] == 0 {
				y++
			}
			if y < length {
				nums[x], nums[y] = nums[y], nums[x]
			} else {
				// 0 放到末尾，长度-1
				length--
			}
		}
	}
}
```

**复杂度分析：**
- 时间复杂度：O(n^2)（最坏情况）
- 空间复杂度：O(1)
