# 18. 四数之和

## 题目信息

| 属性 | 值 |
|------|-----|
| 题号 | [18. 四数之和](https://leetcode.cn/problems/4sum/) |
| 难度 | Medium |
| 分类 | array |
| 标签 | two pointers, sort |
| 文件 | `array/code_18.go` |

## 题目描述

给你一个由 `n` 个整数组成的数组 `nums`，和一个目标值 `target`。请你找出并返回满足要求的四元组 `[nums[a], nums[b], nums[c], nums[d]]` 的个数：

- `0 <= a, b, c, d < n`
- `a`、`b`、`c` 和 `d` 互不相同
- `nums[a] + nums[b] + nums[c] + nums[d] == target`

**示例 1：**

```
输入：nums = [1,0,-1,0,-2,2], target = 0
输出：[[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]
```

**示例 2：**

```
输入：nums = [2,2,2,2,2], target = 8
输出：[[2,2,2,2]]
```

## 解题算法

### 方法：排序 + 双指针

**核心思路：**
在三数之和的基础上增加一层循环。固定前两个数 `nums[i]` 和 `nums[j]`，然后用双指针在剩余子数组中寻找两个数使四数之和等于目标值。

去重策略与三数之和类似，在外层循环和第二层循环中分别跳过重复元素。找到一组解后，也跳过左右两侧的重复元素。

**代码实现：**
```go
func fourSum(nums []int, target int) [][]int {
	sort.Ints(nums)
	n := len(nums)
	var result [][]int

	for i := 0; i < n-3; i++ {
		if i > 0 && nums[i] == nums[i-1] {
			continue
		}
		for j := i + 1; j < n-2; j++ {
			if j > i+1 && nums[j] == nums[j-1] {
				continue
			}
			left, right := j+1, n-1
			for left < right {
				sum := nums[i] + nums[j] + nums[left] + nums[right]
				if sum == target {
					result = append(result, []int{nums[i], nums[j], nums[left], nums[right]})
					for left < right && nums[left] == nums[left+1] {
						left++
					}
					for left < right && nums[right] == nums[right-1] {
						right--
					}
					left++
					right--
				} else if sum < target {
					left++
				} else {
					right--
				}
			}
		}
	}
	return result
}
```

**复杂度分析：**
- 时间复杂度：O(n^3)
- 空间复杂度：O(1)
